@poukai-inc:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Mirror pouk.ai-app — token resolved from env at install time (CI: repo
# secret NPM_TOKEN; local dev: shell-exported GH PAT with read:packages
# on @poukai-inc).
engine-strict=false
