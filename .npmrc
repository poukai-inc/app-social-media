engine-strict=false

# GitHub Packages — @poukai-inc scope
# Requires NPM_TOKEN env var (GitHub PAT with read:packages scope).
# See docs/setup-npm-token.md for the one-time setup instructions.
@poukai-inc:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
